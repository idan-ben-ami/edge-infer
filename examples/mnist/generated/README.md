These files were generated by running:

```bash
uv run python edge_infer.py examples/mnist/mnist_cnn.onnx --output ./mnist_model/ --quantize int8
```

`weights.rs` is omitted (52K parameters as const arrays — not interesting to read).
To generate the full crate yourself, see the quickstart in the root README.
